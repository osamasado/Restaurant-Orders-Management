# Local development profile.
# Spring Boot's docker-compose support auto-starts backend/compose.yaml and
# derives the datasource from that service's own env vars/exposed port -
# no manual `docker compose up` and no datasource properties needed here.

spring.docker.compose.enabled=true

spring.jpa.show-sql=true
