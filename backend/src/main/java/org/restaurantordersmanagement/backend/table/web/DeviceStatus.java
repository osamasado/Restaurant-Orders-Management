package org.restaurantordersmanagement.backend.table.web;

/** Derived, not stored - see TableResponse.from for the lastSeenAt-age threshold. */
public enum DeviceStatus {
    UNPAIRED,
    OFFLINE,
    ONLINE
}
